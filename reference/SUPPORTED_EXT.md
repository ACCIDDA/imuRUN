# Supported input file extensions

The file extensions imurun knows how to read, in precedence order. CSV
takes precedence over RDS when both exist for the same input.

## Usage

``` r
SUPPORTED_EXT
```

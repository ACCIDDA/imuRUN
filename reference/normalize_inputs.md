# Normalize read inputs: friendly headers -\> canonical, and auto obs_id

Normalize read inputs: friendly headers -\> canonical, and auto obs_id

## Usage

``` r
normalize_inputs(result)
```

## Arguments

- result:

  a list with `obs`/`locs` and optionally `target`.

## Value

the normalized list.

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loaders.R
\name{read_workbook}
\alias{read_workbook}
\title{Read all required sheets from an .xlsx workbook}
\usage{
read_workbook(path)
}
\arguments{
\item{path}{character; path to a \code{.xlsx} workbook.}
}
\value{
A named list with elements \code{obs}, \code{pops}, and \code{locs}, each a
\code{data.frame}.
}
\description{
Reads the \code{observations}, \code{populations}, and \code{locations} sheets
from a single Excel workbook into data frames. Reports every missing sheet at
once (mirroring \code{\link[=check_all_inputs]{check_all_inputs()}} semantics) rather than failing on the
first.

Sheet names are matched case-insensitively against the required
\link{IMURUN_SHEETS}. The reader package ('readxl') is only needed at call time;
an informative error is raised if it is not installed.
}
\examples{
wb <- system.file("extdata", "imurun_example.xlsx", package = "imurun")
if (nzchar(wb) && requireNamespace("readxl", quietly = TRUE)) {
  inputs <- read_workbook(wb)
  names(inputs)
}

}

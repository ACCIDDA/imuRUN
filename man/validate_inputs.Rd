% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_inputs}
\alias{validate_inputs}
\title{Validate imurun inputs and report all problems at once}
\usage{
validate_inputs(inputs, max_cohort = NULL, max_age = NULL, max_dose = 2L)
}
\arguments{
\item{inputs}{a named list with \code{obs}, \code{pops}, and \code{locs} (as returned by
\code{\link[=read_inputs]{read_inputs()}}), or a path passed straight to \code{\link[=read_inputs]{read_inputs()}}.}

\item{max_cohort, max_age}{integer upper bounds for the \code{cohort} and \code{age}
columns. Default to the largest value present in \code{populations} so that
validation does not impose a model configuration; supply explicit bounds to
enforce a particular schedule.}

\item{max_dose}{integer; the maximum allowed \code{dose} (default \code{2}).}
}
\value{
Invisibly, a named list of the canonicalized \code{obs}, \code{pops}, and
\code{locs} frames.
}
\description{
A friendly validation layer over the 'imuGAP' canonicalizers
(\code{canonicalize_observations()}, \code{canonicalize_populations()}, and
\code{canonicalize_locations()}). It reports problems in spreadsheet
terms -- naming the offending sheet and, where 'imuGAP' provides it, the
offending column or row -- and collects \emph{all} problems it can find rather
than stopping at the first.

Problems detected include:
\itemize{
\item missing or renamed required columns (per \link{IMURUN_SCHEMA});
\item non-numeric count columns (\code{positive}, \code{sample_n}, \code{cohort}, \code{age},
\code{dose});
\item \code{loc_id} values referenced in \code{populations} but absent from
\code{locations};
\item \code{obs_id} mismatches between \code{observations} and \code{populations};
\item \code{dose}, \code{cohort}, and \code{age} values out of range;
\item structural location problems (duplicate or missing root, cycles).
}

On success the canonicalized frames are returned invisibly. On failure a
single error is raised whose message lists every problem found.
}
\examples{
wb <- system.file("extdata", "imurun_example.xlsx", package = "imurun")
if (nzchar(wb) && requireNamespace("readxl", quietly = TRUE)) {
  validate_inputs(read_inputs(wb))
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\docType{data}
\name{IMURUN_SCHEMA}
\alias{IMURUN_SCHEMA}
\title{Canonical imuGAP input schema}
\format{
A named list of three character vectors (\code{observations},
\code{populations}, \code{locations}), each giving the required column names in
schema order.
}
\usage{
IMURUN_SCHEMA
}
\description{
The column schema that imurun targets for each of the three
inputs. These are the columns required by the 'imuGAP' canonicalizers
(\code{canonicalize_observations()}, \code{canonicalize_populations()}, and
\code{canonicalize_locations()} from 'imuGAP') and reflected in the shipped
template and example workbooks. Extra columns are permitted and ignored by
'imuGAP'; only the required columns are enforced.

Each element of \code{IMURUN_SCHEMA} describes one sheet/input:
\describe{
\item{observations}{One row per observation. Required columns: \code{obs_id}
(unique non-missing identifier), \code{positive} (non-negative integer count
of positive results), \code{sample_n} (positive integer sample size, with
\code{positive <= sample_n}). Optional: \code{censored} (\code{NA} or \code{1}).}
\item{populations}{Cohort/age/dose breakdown that each observation covers,
one or more rows per \code{obs_id}. Required columns: \code{obs_id} (must match the
observations), \code{loc_id} (must exist in locations), \code{cohort} (positive
integer), \code{age} (positive integer), \code{dose} (integer in \code{1:max_dose}).
Optional: \code{weight} (positive numeric summing to 1 within each \code{obs_id}).}
\item{locations}{The location hierarchy. Required columns: \code{loc_id}
(unique identifier), \code{parent_id} (the parent's \code{loc_id}, or \code{NA} for the
single root).}
}
}
\examples{
IMURUN_SCHEMA$observations
IMURUN_SCHEMA$populations
IMURUN_SCHEMA$locations

}
\seealso{
\code{\link[=validate_inputs]{validate_inputs()}}, \code{\link[=read_inputs]{read_inputs()}}, \code{\link[=imurun_template]{imurun_template()}}
}
\keyword{datasets}

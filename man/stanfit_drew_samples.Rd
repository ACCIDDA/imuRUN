% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{stanfit_drew_samples}
\alias{stanfit_drew_samples}
\title{Did a stanfit actually draw posterior samples?}
\usage{
stanfit_drew_samples(stanfit)
}
\arguments{
\item{stanfit}{the \code{stanfit} object carried by an \code{imugap_fit}
(i.e. \code{fit$stanfit}). A \code{NULL} or otherwise malformed value yields \code{FALSE}
rather than an error.}
}
\value{
Logical scalar; \code{TRUE} only when the sampler completed (\verb{@mode == 0})
and stored at least one draw.
}
\description{
\code{\link[imuGAP:sampling]{imuGAP::sampling()}} returns an \code{imugap_fit} wrapper even when
the underlying Stan sampler fails to initialize -- it prints the Stan
exception and returns a \code{stanfit} in mode \code{2} with an empty \verb{@sim} slot,
rather than raising an R error. This helper distinguishes a genuine fit
(samples drawn) from that silent-failure case so \code{\link[=run_fit]{run_fit()}} can report the
failure instead of saving an empty \code{fit.rds}.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets.R
\name{summarize_targets}
\alias{summarize_targets}
\title{Reduce posterior draws to a median and credible interval per target}
\usage{
summarize_targets(draws, ci_level = 0.95)
}
\arguments{
\item{draws}{data.frame of posterior draws with (at least) an \code{obs_id} key
and a \code{p_obs} column, one row per (draw x target). Any of the identity
columns \code{target_id}, \code{loc_id}, \code{cohort}, \code{age}, \code{dose} present are carried
through to the output.}

\item{ci_level}{numeric in \verb{(0, 1)}; the credible-interval level (default
\code{0.95}, i.e. the 2.5\% and 97.5\% quantiles).}
}
\value{
A data.frame with one row per target: the carried identity columns
plus \code{n_draws}, \code{est_median}, \code{est_lower}, \code{est_upper}, and \code{ci_level}.
}
\description{
Collapses the long posterior draws returned by 'imuGAP''s
by-target prediction path into one wide row per target: the posterior median
and a symmetric credible interval of the predicted coverage probability
(\code{p_obs}). 'imuGAP' returns draws only; this is the small reduction imurun
layers on top.
}
\examples{
draws <- data.frame(
  obs_id = 1L, loc_id = "A", cohort = 5L, age = 5L, dose = 2L,
  p_obs = seq(0, 1, length.out = 201)
)
summarize_targets(draws, ci_level = 0.95)

}
\seealso{
\code{\link[=expand_targets]{expand_targets()}}
}

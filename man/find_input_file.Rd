% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loaders.R
\name{find_input_file}
\alias{find_input_file}
\title{Find and read a named input from a directory}
\usage{
find_input_file(dir, name)
}
\arguments{
\item{dir}{character; directory to look in.}

\item{name}{character; the input base name, without extension.}
}
\value{
The object read from the matching file.
}
\description{
Looks for a named input (e.g. \code{"observations"}) in a directory,
trying each supported extension in precedence order (CSV before RDS), and
reads the first match with \code{\link[=load_by_ext]{load_by_ext()}}. Errors with a clear message if
no matching file is found.
}
\examples{
dir <- tempfile("imurun_find_")
dir.create(dir)
write.csv(data.frame(positive = 1:3, sample_n = 10:12),
          file.path(dir, "observations.csv"), row.names = FALSE)
find_input_file(dir, "observations")

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets.R
\name{validate_targets}
\alias{validate_targets}
\title{Validate a target-request sheet with friendly, row-referenced errors}
\usage{
validate_targets(targets, loc_ids, max_cohort, max_age, max_dose = 2L)
}
\arguments{
\item{targets}{data.frame of target-request rows.}

\item{loc_ids}{character; the known location identifiers (the \code{loc_id} column
of the locations sheet).}

\item{max_cohort, max_age}{integer upper bounds for \code{cohort} and the age span.}

\item{max_dose}{integer; the maximum allowed \code{dose} (default \code{2}).}
}
\value{
Invisibly, the validated \code{targets} data.frame.
}
\description{
Checks a \code{target} sheet the same way \code{\link[=validate_inputs]{validate_inputs()}} checks
the input sheets: it collects \emph{all} problems and reports them at once in
spreadsheet terms, naming the offending column and row. Problems detected
include missing/renamed required columns (per \link{IMURUN_TARGET_SCHEMA}),
non-numeric \code{cohort}/\code{age_low}/\code{age_high}/\code{dose}, \code{loc_id} values absent from
the locations sheet, an inverted age span (\code{age_low > age_high}), and
out-of-range \code{cohort}/\code{age}/\code{dose}.

On success the (unmodified) target frame is returned invisibly. On failure a
single error is raised whose message lists every problem found.
}
\examples{
tg <- data.frame(loc_id = "A;B", cohort = 5, age_low = 5, age_high = 7)
validate_targets(tg, loc_ids = c("A", "B"), max_cohort = 15, max_age = 8)

}
\seealso{
\code{\link[=expand_targets]{expand_targets()}}, \code{\link[=validate_inputs]{validate_inputs()}}
}
